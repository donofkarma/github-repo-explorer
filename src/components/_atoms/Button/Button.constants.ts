const AS = {
  A: 'a',
  BUTTON: 'button',
} as const;

const TYPES = {
  BUTTON: 'button',
  RESET: 'reset',
  SUBMIT: 'submit',
};

const VARIANTS = {
  LINK: 'link',
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
};

export { AS, TYPES, VARIANTS };
